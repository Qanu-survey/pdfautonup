\documentclass{article}

\usepackage{calc}
\usepackage[paperwidth=7cm,paperheight={29.7cm/2}]{geometry}
\usepackage{tikz}
\newfont{\HUGE}{cmr10 at 250pt}%

\pagestyle{empty}

\begin{document}

\foreach \i in {1, 2, ..., 2} {

  \begin{tikzpicture}[overlay,remember picture,line width=5pt, color=red]
    \draw (current page.north west) rectangle (current page.south east);
    \draw (current page.north west) -- (current page.south east);
    \draw (current page.south west) -- (current page.north east);
    \draw (current page.center) node[lightgray]{\HUGE \i};
  \end{tikzpicture}

  \pagebreak
}

\end{document}
