TODO Quick description

Install
=======

You can run the program without installing it using `bin/pdfautonup`. To
install it, run:

  python3 setup.py install

Documentation
=============

You can build the documentation using:

  cd doc && make html
